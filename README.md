# Web-App-Geocoder
 
This web app allows the user to upload a CSV file containing addresses in the 2nd column and then download a new file containing 2 new columns containing the coordinates to those addresses. 
